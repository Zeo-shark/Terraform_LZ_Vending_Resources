variable "default_budget_config" {
  description = "Default budget configuration"
  type        = map(map(string))
  # Define default budget configurations here
}

# Define default budgets using the variable
